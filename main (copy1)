#project #3
# create 1-D list with numbers 20-30
numbers_1d = list(range(20, 31))

# calculate sum and average of the numbers
total = sum(numbers_1d)
average = total / len(numbers_1d)

# retrieve and print the lucky number 27
lucky_number = numbers_1d[7]
print("Lucky number:", lucky_number)

# create 2-D list with numbers 1-10
numbers_2d = [[j for j in range(1, 11)] for i in range(1)]

# retrieve and print the lucky number 7
lucky_number_2d = numbers_2d[0][6]
print("Lucky number in 2-D list:", lucky_number_2d)

# create a list of 5 random names and print them using a for loop
import random
names = ["Alice", "Bob", "Charlie", "David", "Eve"]
random.shuffle(names)  # shuffle the list of names randomly
for name in names:
    print(name)
